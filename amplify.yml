version: 1
env:
  variables:
    key: value
frontend:
  phases:
    preBuild:
      - npm ci
    build:
      commands:
        - npm run build
    artifacts:
      baseDirectory: public
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
